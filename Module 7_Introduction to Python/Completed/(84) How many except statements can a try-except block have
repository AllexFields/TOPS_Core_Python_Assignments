How many except statements can a try-except block have?


A single try block in Python can have multiple except statements.
Multiple except blocks are used when we want to handle different types of exceptions separately.